# (1).= adalah operator penugasan (assignment), digunakan untuk memberi nilai ke sebuah variabel.
# == adalah operator perbandingan (comparison), digunakan untuk membandingkan dua nilai, apakah sama atau tidak.
#Contoh:# Penugasan (assignment)
#x = 5      # x sekarang bernilai 5

# Perbandingan (comparison)
#print(x == 5)   # True, karena x memang bernilai 5
#print(x == 10)  # False, karena x tidak bernilai 10

# (2.)Jawaban yang TIDAK VALID: c) 2x

# (3.) Jawaban: b) Program akan menampilkan 1010

# (4.)# Meminta pengguna memasukkan alas segitiga dan mengubahnya menjadi float
alas = float(input("Masukkan alas segitiga: "))
# Meminta pengguna memasukkan tinggi segitiga dan mengubahnya menjadi float
tinggi = float(input("Masukkan tinggi segitiga: "))
# Menghitung luas segitiga dengan rumus: 0.5 * alas * tinggi
luas = 0.5 * alas * tinggi
# Menampilkan hasil luas ke layar
print("Luas segitiga adalah:", luas)
